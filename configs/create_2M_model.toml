[io]
# IMPORTANT: Point to the filtered output from the previous step
smiles_file = "data/2M_filtered.smi"
model_file = "lohc_2M.model"

[network]
num_layers = 3
layer_size = 512
dropout = 0.1
layer_normalization = false
standardize = true

[metadata]
data_source = "data/2M.csv"
comment = "Model created from filtered 2M dataset (Si, B, P, Se, As, Ge supported)"